<!--pages/risk_test/risk_test9.wxml-->
<view class="window">
  <text>
    10、以下哪项描述最符合您的投资态度：
  </text>
  
  <view class="choices">
    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ11"
            hover-class="button-hover"> A、厌恶风险，不希望本金损失，希望获得稳定回报
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ11"
            hover-class="button-hover"> B、保守投资，不希望本金损失，愿意承担一定幅度的收益波动
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ11"
            hover-class="button-hover"> C、寻求资金的较高收益和成长性，愿意为此承担有限本金损失
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ11"
            hover-class="button-hover"> D、希望赚取高回报，愿意为此承担较大本金损失
    </button>
  </view>
</view>
