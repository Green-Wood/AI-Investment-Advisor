<!--pages/risk_test/risk_test10.wxml-->
<view class="window">
  <text>
    11、假设有两种投资：投资A预期获得10%的收益，可能承担的损失非常小；投资B预期获得30%的收益，但可能承担较大亏损。您会怎么支配您的投资：
  </text>
  
  <view class="choices">
    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ12"
            hover-class="button-hover"> A、全部投资于收益较小且风险较小的A
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ12"
            hover-class="button-hover"> B、同时投资于A和B，但大部分资金投资于收益较小且风险较小的A
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ12"
            hover-class="button-hover"> C、同时投资于A和B，但大部分资金投资于收益较大且风险较大的B
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ12"
            hover-class="button-hover"> D、全部投资于收益较大且风险较大的B
    </button>
  </view>
</view>
