<!--pages/risk_test/risk_test11.wxml-->
<view class="window">
  <text>
   12、您认为自己能承受的最大投资损失是多少：
  </text>
  
  <view class="choices">
    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ13"
            hover-class="button-hover"> A、10%以内
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ13"
            hover-class="button-hover"> B、10%-30%
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ13"
            hover-class="button-hover"> C、30%-50%
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ13"
            hover-class="button-hover"> D、超过50%
    </button>
  </view>
</view>
