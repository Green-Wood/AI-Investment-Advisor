<!--pages/risk_test/risk_test7.wxml-->
<view class="window">
  <text>
    8、您计划的投资期限是多久：
  </text>
  
  <view class="choices">
    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ9"
            hover-class="button-hover"> A、1年以下
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ9"
            hover-class="button-hover"> B、1至3年
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ9"
            hover-class="button-hover"> C、3至5年
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ9"
            hover-class="button-hover"> D、5年以上
    </button>
  </view>
</view>
