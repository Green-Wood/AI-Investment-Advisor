<!--pages/risk_test/risk_test4.wxml-->
<view class="window">
  <text>
    5、您的投资知识可描述为：
  </text>
  
  <view class="choices">
    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ6"
            hover-class="button-hover"> A、有限：基本没有金融产品方面的知识
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ6"
            hover-class="button-hover"> B、一般：对金融产品及其相关风险具有基本的知识和理解
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ6"
            hover-class="button-hover"> C、丰富：对金融产品及其相关风险具有丰富的知识和理解
    </button>

  </view>
</view>
