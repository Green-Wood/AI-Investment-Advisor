<!--pages/risk_test/risk_test2.wxml-->
<view class="window">
  <text>
    3、在您每年的家庭可支配收入中，可用于金融投资(储蓄存款除外)的比例为：
  </text>
  
  <view class="choices">
    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ4"
            hover-class="button-hover"> A、小于10%
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ4"
            hover-class="button-hover"> B、10%至25%
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ4"
            hover-class="button-hover"> C、25%至50%
    </button>

    <button type="setDefault" size="setDefault" loading="{{loading}}" plain="{{plain}}"
            disabled="{{disabled}}" bindtap="jumpToQ4"
            hover-class="button-hover"> D、大于50%
    </button>
  </view>
</view>
